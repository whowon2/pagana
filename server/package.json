{
  "name": "server",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build src/index.ts --compile --outfile app"
  },
  "dependencies": {
    "hono": "^4.11.3"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
