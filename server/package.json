{
  "name": "server",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "lint": "biome check",
    "format": "biome format --write"
  },
  "dependencies": {
    "hono": "^4.11.3"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.10",
    "@types/bun": "latest"
  }
}
